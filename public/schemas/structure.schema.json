{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": ["etapes", "lunettes"],
  "properties": {
    "meta": {
      "type": "string",
      "description": "Chemin vers le fichier contenant les métadonnées générales du système (ex. meta.json)."
    },
    "introduction_et_mission": {
      "type": "string",
      "description": "Chemin vers le fichier d'introduction et de mission (ex. introduction_et_mission.json)."
    },
    "etapes": {
      "type": "array",
      "description": "Liste des fichiers d'étapes utilisés dans le système.",
      "items": {
        "type": "string",
        "description": "Chemin relatif vers un fichier d'étape (JSON)."
      }
    },
    "lunettes": {
      "type": "array",
      "description": "Index des groupes de lunettes, chacun représentant un fichier JSON avec une famille de lunettes.",
      "items": {
        "type": "object",
        "required": ["famille", "fichier", "nb"],
        "properties": {
          "famille": {
            "type": "string",
            "description": "Nom de la famille (ex: 🧠 Biais cognitifs)."
          },
          "fichier": {
            "type": "string",
            "description": "Chemin relatif vers le fichier JSON correspondant à cette famille."
          },
          "nb": {
            "type": "integer",
            "minimum": 1,
            "description": "Nombre d’entrées dans le fichier."
          }
        },
        "additionalProperties": false,
        "description": "Entrée d'index pour un fichier de lunettes."
      }
    },
    "ressources": {
      "type": "object",
      "description": "Références vers des fichiers ressources supplémentaires (axes, masques, compagnons, etc.).",
      "properties": {
        "compagnons_symboliques": { "type": "string" },
        "axes_autistiques": { "type": "string" },
        "lunettes_subjectives": { "type": "string" },
        "masques_symboliques": { "type": "string" }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": true,
  "description": "Schéma de la structure globale du système Prompt Ultime, incluant les étapes, les lunettes et les fichiers annexes."
}

